# Welcome to Tango-Controls server ${rootArtifactId}

## Getting started

Clone this repo and build single executable jar using maven:

```bash
$> mvn package
```

Run the server:

```bash
$> java -jar target/${rootArtifactId}-${version}.jar

```